'use strict';

describe('rantly.rants module', function() {

  beforeEach(module('rantly.rants'));

  describe('rants controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rantsCtrl = $controller('rantsCtrl');
      expect(rantsCtrl).toBeDefined();
    }));

  });
});