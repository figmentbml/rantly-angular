'use strict';

describe('rantly.users module', function() {

  beforeEach(module('rantly.users'));

  describe('users controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var usersCtrl = $controller('UsersCtrl');
      expect(usersCtrl).toBeDefined();
    }));

  });
});
